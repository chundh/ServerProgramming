package com.rubypaper.biz.common;

public class InsertBoardTest {
    public static void main(String[] args) {
    	// 1. 글 등록 기능 처리
    	BoardDAO boardDAO = new BoardDAO();
    	boardDAO.insertBoard("DAO제목", "DAO", "DAO 내용");
    	
    	// 2. 글 목록 검색 기능 처리
    	boardDAO.getBoardList();
		/*
		 * Connection conn = null; // Statement stmt = null; PreparedStatement stmt =
		 * null; try{ conn = JDBCUtil.getConnection();
		 * 
		 * String sql = "insert into board(seq, title, writer, content) " +
		 * "values((select nvl(max(seq), 0)+1 from board), ?, ?, ?)"; stmt =
		 * conn.prepareStatement(sql);
		 * 
		 * //파라미터 세팅 stmt.setString(1, "JDBC 제목"); stmt.setString(2, "테스터");
		 * stmt.setString(3, "JDBC 내용...");
		 * 
		 * int cnt = stmt.executeUpdate(); } catch (SQLException throwables) {
		 * throwables.printStackTrace(); }finally { JDBCUtil.close(stmt, conn); }
		 */
    }
}
